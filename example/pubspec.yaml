name: rgb_form_kit_example
description: Example app for rgb_form_kit
publish_to: none

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  rgb_form_kit:
    path: ..
  flutter_form_builder: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
